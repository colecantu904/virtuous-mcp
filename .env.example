# Virtuous CRM+ API key (Application Key / Bearer token).
# Create one in Virtuous: Settings -> All Settings -> Connectivity -> Application Keys
VIRTUOUS_API_KEY=your_api_key_here

# Optional: override the API base URL (defaults to the value below)
# VIRTUOUS_BASE_URL=https://api.virtuoussoftware.com
